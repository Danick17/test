{
  "name": "bestmark-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor native shell for the Bestmark web app (iOS App Store / Google Play).",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios",
    "android": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/android": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
